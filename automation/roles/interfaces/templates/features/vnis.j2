{% if comments|default(False) %}
###############
# VNIs
###############
{{ vni.profiles | default() | to_nice_yaml | comment(prefix='# vni.profiles:', postfix='') -}} 
{% endif %}
{% for profile in vni.profiles %}
{%   for vni in vni_profiles|selectattr('name', 'equalto', profile) %}
{%     if comments|default(False) %}
{{ vni | default() | to_nice_yaml | comment(prefix='# vni:', postfix='') -}}
{%     endif %}
auto {{ vni.name }}
iface {{ vni.name }}
    bridge-access {{ vni.vlan_id }}
    vxlan-id {{ vni.vxlan_id }}
{%     for extra in vni.extras|default() %}
    {{ extra }}
{%     endfor %}

{%   endfor %}
{% endfor %}

